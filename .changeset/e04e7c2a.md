---
"@vndv/pi-codegraph": patch
---

Fix per-call MCP session hangs and reduce file-watcher overhead

- Add `--no-watch` flag to `codegraph serve` to disable constant file-watcher auto-syncs
- Add 20-second session timeout with clean child-kill to prevent infinite "working..." hangs
- Add Git Bash (`/c/...`) and WSL (`/mnt/c/...`) path normalization in `resolveProjectCwd`
- Extract `toFileUriPath` helper for correct Windows `file://` URI generation

This PR was created with the assistance of an AI coding agent.